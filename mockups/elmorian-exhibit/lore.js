// Every word the exhibit says lives in data/lore/elmorian.json, which both
// the itch build and the desktop build read (CLAUDE.md 4.4: data belongs in
// data files). The build inlines it here so the page stays one file.
window.LORE = {{LORE_JSON}};
